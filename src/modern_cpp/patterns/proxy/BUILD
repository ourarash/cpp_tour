load("@rules_cc//cc:defs.bzl", "cc_binary")
cc_binary(
    name = "main",
    srcs = ["main.cc"],
    deps = ["//src/lib:utility"],
    # Teaching skeleton — Image/ImageProxy declarations only, no main().
    tags = ["manual"],
)

load("@rules_cc//cc:defs.bzl", "cc_binary")
cc_binary(
    name = "main",
    srcs = ["main.cc"],
    deps = ["//src/lib:utility"],
    # Teaching skeleton — Shape/TextView interface methods are declared
    # but not defined. Read for the pattern; doesn't link.
    tags = ["manual"],
)
